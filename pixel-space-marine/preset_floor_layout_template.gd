class_name PresetFloorLayoutTemplate
extends PresetFloorLayout
## 推荐给各 Sublevel 使用的脚本基类：`extends PresetFloorLayoutTemplate`。
## 与 `PresetFloorLayout` 等价；多张地图共用语义图块时，将 `DungeonMarkerSemanticsConfig`
## （例如 `Levels/default_marker_semantics.tres`）挂到导出项 `marker_bundle`，改一处同步所有引用该资源的关卡。
